# Python setup

## Install python3 locally (venv setup)

```bash
python -m venv venv
```

## Go into venv environment

```bash
source venv/bin/activate {dir to activate}
```

## Install pyspark locally (in venv)

```bash
pip install pyspark
```
